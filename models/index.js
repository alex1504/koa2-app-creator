const User = require('./user.model')
const {
    initModels,
    updateModels
} = require('../libs/sequelize')

// 初始化schema
// initModels()

// 更新schema
// updateModels()

module.exports = {
    User
}